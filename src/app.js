const moveTop = document.querySelector("#button");

moveTop.addEventListener("click", () => {
    const msg = "email not implemented";
    window.scrollTo({
        top: 0,
        left: 0,
    });
    console.log(msg);
});
